# Cloudflare Pages — frontend static site
# Note: Pages config does not support account_id; use CLOUDFLARE_ACCOUNT_ID env instead.
name = "xone-wallet-web"
compatibility_date = "2025-08-10"
pages_build_output_dir = "dist"
